# Общий контекст

## О пользователе

**Профиль:** Владелец небольшой компании — агент по банковским гарантиям.

**Что делает бизнес:**
- Находит лидов: поставщики госзаказа (44-ФЗ) и коммерческих договоров (223-ФЗ), которым нужна банковская гарантия.
- Собирает у клиента документы.
- Заводит заявку в банк (банков несколько, у каждого свои требования).
- Если банк одобряет — выдаёт БГ, получает комиссию от банка.
- Комиссия: 20–45% от банковской комиссии по выданной БГ.

**Объём:** 10–50 заявок/клиентов в месяц.

**Роль пользователя в работе со мной:** визионер, не кодер. Описывает что нужно — я планирую и реализую. Сам код не пишет.

## Текущие болевые точки (по убыванию важности)

1. **Сбор и проверка документов клиента** — много времени, бумажная работа.
2. **Поиск лидов** — мониторинг тендеров и поставщиков.
3. **Сопровождение заявок, отчётность, финансы** — учёт статусов, комиссий.

## Где сейчас хранятся данные

- Какая-то CRM (точное название уточнить позже).
- Документы — отдельно, в папках/мессенджерах.

## Рабочая среда (для меня, Claude)

- На Mac у пользователя — локальный Claude Code + личный Obsidian-vault (туда я не имею доступа).
- В облаке (текущая среда) — мой «vault» в этом репо `skyworkerq-hash/gh`, папка `claude-vault/`.
- Основная ветка для работы: **`main`** (создана 2026-05-15, содержит всю накопленную работу).
- Историческая ветка `claude/ai-capabilities-overview-vWBGX` синхронизирована с main.
- Default branch на GitHub: на момент написания всё ещё `claude/ai-capabilities-overview-vWBGX` — пользователь должен переключить вручную в Settings → Branches.
- Network access у Environment Default — **Full** (любые внешние API доступны).
- Репо должно быть **приватным** (пользователь подтвердил требование).
- **Быстрая команда восстановления контекста: `/м` или `/память`** — описана в `.claude/commands/`.

## Установленные community-скилы (через SessionStart-хук)

При старте каждой облачной сессии хук `.claude/hooks/install-community-skills.sh` подключает ~154 скила из 13 репозиториев. Главное по доменам:

- **Планирование/архитектура:** `superpowers-brainstorming`, `superpowers-writing-plans`, `planning-with-files-plan`, `planning-with-files-status`.
- **Маркетинг (40 скилов):** `marketing-*` — copywriting, SEO, ads, лидген, email, CRO, цены, лендинги. Под бизнес пользователя по БГ это пригодится для собственного маркетинга агентства.
- **Документы:** `composio-document-pdf`, `-docx`, `-pptx`, `-xlsx` — обработка/генерация документов клиентов (главная боль пользователя!).
- **Браузер/парсинг:** `agent-browser-agent-browser` — автоматизация веб-сайтов (под мониторинг zakupki.gov.ru).
- **Текст:** `humanizer` — очистка текстов от AI-духа.
- **Дизайн:** `anthropic-frontend-design`, `vercel-web-design-guidelines`, `impeccable-impeccable`.
- **Видео:** `remotion`.
- **Память:** `supermemory-supermemory` (MCP — отдельно на Mac).
- **Безопасность/код:** Trail of Bits (73 скила, в основном про аудит кода — пользователю мало релевантно).
- **Понимание кода:** `understand-anything-*` (тоже больше для кодеров).

## Активные проекты

- **`pdf-editor`** — single-file веб-приложение для работы с PDF договорами + AI-извлечение реквизитов через DeepSeek. **Задеплоено: <https://skyworkerq-hash.github.io/pdf-editor/>**. Источник правды разработки — `apps/pdf-editor/index.html` в этом репо; деплой — копия в публичном репо `skyworkerq-hash/pdf-editor`.
- **`tender-monitor`** — ежедневный мониторинг тендеров с zakupki.gov.ru. На паузе, ждёт 4 ответа от пользователя.

## Что НЕ делаем сейчас

- База знаний по банкам (RAG для подбора банка под клиента) — отложено.
- CRM-замена — пользуется существующей.
- Автодеплой `pdf-editor` из основного репо в публичный — пока вручную через Upload files в UI.

## Долгосрочное видение

Набор личных AI-инструментов под бизнес банковских гарантий: поиск лидов → квалификация → сбор документов → подбор банка → сопровождение заявки → учёт комиссий. По одному инструменту за раз.
